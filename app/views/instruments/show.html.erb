<p>
  <b><%=h @instrument.name %></b>
</p>

<p>
  <b>Brand:</b>
  <%=h @instrument.brand.andand.name %>
</p>


connected to:<br>
<% @devices.each_with_index do |d, i| %>
  <%= link_to_name d %> <%= link_to 'add to configuration', new_element_path(:element => {:specification_type => 'Connection', :specification_id => @connections[i].id}) %><br>
<% end %>
outputs:<br>
<% @instrument.outputs.each do |o| %>
  <%= link_to_name o %><br>
<% end %>

<%= link_to 'new output', new_output_path(:output => {:host_type => 'Instrument', :host_id => @instrument.id}) %><br>
knobs:<br>
<% @instrument.knobs.each do |k| %>
  <%= link_to_name k %><br>
<% end %>
<%= link_to 'new knob', new_knob_path(:knob => {:host_type => 'Instrument', :host_id => @instrument.id}) %>
<br>
inputs:<br>
<% @instrument.inputs.each do |o| %>
  <%= link_to_name o %><br>
<% end %>

<%= link_to 'new input', new_input_path(:input => {:host_type => 'Instrument', :host_id => @instrument.id}) %><br>

<br>
<%= link_to 'Edit', edit_instrument_path(@instrument) %> |
<%= link_to 'Back', instruments_path %>
